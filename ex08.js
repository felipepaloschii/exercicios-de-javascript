function duas_variaveis() {

    let A = 7;
    let B = 12;

    alert("A: " + A);
    alert("B: " + B );

    alert("Diferença entre A e B: " + (A-B));
    alert("O dobro de A somado ao triplo da B: " + (A+A) + (B+B+B));
    alert("Multiplicação de A e B: " + (A*B));
    
}